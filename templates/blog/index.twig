{% extends "index" %}

{% block content %}

<section class="pt-4">
	<div class="container">
		<div class="row">
			<div class="col-12">
				<div class="card card-overlay-bottom h-300 overflow-hidden text-center" style="background-image:url({{ entry.featuredImage.one().getUrl('catHero') }}); background-position: center left; background-size: cover;">
					<!-- Card Image overlay -->
					<div class="card-img-overlay d-flex align-items-center p-3 pb-4 px-sm-5"> 
						<div class="col-12 mt-auto d-md-flex justify-content-between align-items-center">
							<h1 class="text-white display-5">{{ entry.title }}</h1>
							<nav class="d-flex justify-content-center" aria-label="breadcrumb">
						<ol class="breadcrumb breadcrumb-dots mb-0">
							<li class="breadcrumb-item"><a href="{{ siteUrl }}"><i class="bi bi-house me-1"></i> Home</a></li>
							<li class="breadcrumb-item active">All posts</li>
						</ol>
					</nav>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<!-- =======================
Main content START -->
<section class="position-relative pt-0">
	<div class="container">
		<div class="row">
			{% set blogs = craft.entries.section('blog').all() %}
            {% for blog in blogs %} 
            {% set photo = blog.featuredImage.one() %}
			<!-- Card item START -->
			<div class="col-sm-6 col-lg-3">
				<div class="card mb-4">
					<!-- Card img -->
					<div class="card-fold position-relative">
						<img class="card-img" src="{{ blog.featuredImage.one().getUrl('catMini') }}" alt="{{ blog.title }}">
					</div>
					<div class="card-body px-0 pt-3">
						<h4 class="card-title"><a href="{{ blog.url }}" class="btn-link text-reset stretched-link fw-bold">{{ blog.title }}</a></h4>
						<!-- Card info -->
						<ul class="nav nav-divider align-items-center text-uppercase small">
							<li class="nav-item">
								<a href="{{ blog.url }}" class="nav-link text-reset btn-link">{{ blog.author }}</a>
							</li>
							<li class="nav-item">{{blog.postDate|date('d M Y')}}</li>
						</ul>
					</div>
				</div>
			</div>
			<!-- Card item END -->
            {% endfor %}
			
		</div> <!-- Row end -->

    
	</div>
</section>
<!-- =======================
Main content END -->





 {% endblock %}






